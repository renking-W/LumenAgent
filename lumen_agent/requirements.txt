# Web 框架与 OpenAPI（/docs、请求体验证、路由）
fastapi>=0.115.0

# ASGI 服务器（本地运行、reload、HTTP/1.1）
uvicorn[standard]>=0.32.0

# 读取环境变量与 .env（config.py 的 Settings）
pydantic-settings>=2.6.0

# SQLite 会话（aiosqlite）
aiosqlite>=0.20.0

# web_search 工具（DuckDuckGo 搜索，无需 API Key）
ddgs>=0.5.0

# web_fetch 工具（异步 HTTP 客户端）
httpx>=0.27.0

# web_fetch 工具：HTML → Markdown 转换
html2text>=2024.2.26

# web_fetch 工具：HTML 解析（html2text 的回退方案）
beautifulsoup4>=4.12.0

# SKILL 系统：解析 SKILL.md 的 YAML frontmatter
pyyaml>=6.0

# Agent token 计数（DeepSeek 当前用 cl100k_base 近似；后续多模型在此扩展）
tiktoken>=0.7.0

# 知识库 / RAG 向量存储
chromadb>=0.5.0

# 定时任务调度（AsyncIOScheduler、CronTrigger、IntervalTrigger）
apscheduler>=3.10.0

# CLI 交互式提示（chat_in_cli、knowledge_in_cli）
questionary>=2.1.0

# Web 框架（前端静态文件代理）
flask>=3.0.0

# SSH 连接管理（虚拟机远程命令执行）
paramiko>=3.4.0

# MCP 协议支持（外部工具扩展）
mcp>=1.0.0

# 文件上传支持（FastAPI UploadFile 依赖）
python-multipart>=0.0.9